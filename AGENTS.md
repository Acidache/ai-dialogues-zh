# 网站维护约束

本仓库遵循 Ponytail 原则：先理解完整路径，再选择能够正确工作的最小方案。

- 新功能先问是否真的需要；不为假设中的未来需求预建框架、配置或抽象。
- 优先复用现有 HTML、CSS、JavaScript 和 GitHub Pages 能力；不引入 React、Vue、构建器或 npm 依赖，除非现有方案已被验证无法满足明确需求。
- 新增一期可以复制最接近的现有页面；不要为了消除内容页的表面重复而建立模板系统。
- 公共功能必须在共享文件或共同执行路径中修复，不要给每一期分别打补丁。
- 删除优于新增；原生 HTML/CSS 优于 JavaScript；已有依赖优于新增依赖。
- 一次性缓存参数、兼容分支和临时补丁必须注明删除条件，并在条件满足后移除。
- 每次修改只触碰必要文件，保留原始对话、稳定的 Giscus `data-term`、无障碍基础和安全边界。
- 发布前至少检查：`git diff --check`、站内路径、四张封面、RSS、sitemap、评论入口及移动端基本布局。
- 新增一期时同步更新：首页索引、`feed.xml`、`sitemap.xml`、结构化数据和 `README.md` 当前收录列表。

判断冲突时采用最短且可验证的正确方案；不要用“以后可能需要”扩大当前改动。
